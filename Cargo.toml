[package]
name = "pomodoro-leptos-csr"
version = "0.1.0"
edition = "2024"

[dependencies]
console_error_panic_hook = "0.1.7"
leptos = { version = "0.8.19", features = ["csr"] }
gloo-timers = { version = "0.3", features = ["futures"] }
